# sakumon-system 現状調査レポート

- 調査日: 2026-09-09
- 対象: `main` ブランチ HEAD = `f94c1e1`（"コミット漏れ_0905"）
- 方法: `backend/` `frontend/` の全ソースを実読、`data/sakumon.db`（+ WAL/SHM）をスクラッチにコピーして `sqlite3` で照会
- 注意: コードは一切変更していない。DB照会も元ファイルには触れていない（コピーに対して実行）。

推測は排除し、確認できなかった項目は各節末に「未確認」として明記した。

---

## 1. 全体構成

### 1.1 ディレクトリ構成とファイル一覧（主要ファイルのみ・行数付き）

```
sakumon-system/
├── backend/
│   ├── main.py            314 行  FastAPI 本体・全ルーティング・作問/対話の分岐・信号機/表示種別の決定論的計算
│   ├── database.py        349 行  SQLite 操作（セッション・ログ・停滞カウント・管理者用集計）
│   ├── ai_dialogue.py     318 行  児童向け声かけ生成・figure/target_structure 生成・hint3テープ図のコード生成
│   ├── ai_judge.py        105 行  作問文の構造同定のみ（valid / structure / issue）
│   ├── ai_classify.py      84 行  入力を「作問(sakumon)」か「対話(taiwa)」に1コールで分類
│   ├── kanji_rule.py        9 行  文字づかい共通ルール文字列 KANJI_RULE の定義のみ
│   ├── requirements.txt     4 行  fastapi / uvicorn[standard] / anthropic / python-dotenv
│   └── venv/                     Python 3.14 の仮想環境（コミット対象外）
├── frontend/
│   ├── index.html         116 行  児童向けUI（5画面を1ファイルに内包）
│   ├── index.css          598 行
│   ├── index.js           591 行  児童向けUIの全ロジック・テープ図SVG生成
│   ├── admin.html          50 行  管理者画面（2ビューを1ファイルに内包）
│   ├── admin.css          223 行
│   └── admin.js           295 行  管理者画面の全ロジック
├── data/
│   ├── sakumon.db                SQLite 本体（32 KB）
│   ├── sakumon.db-wal            WALファイル（約 515 KB・未チェックポイント）
│   ├── sakumon.db-shm
│   ├── sakumon.db.backup_20260721_160059   （Git 未追跡）
│   └── sakumon.db.backup_20260722_230542   （Git 未追跡）
├── .env                          ANTHROPIC_API_KEY のみ（1行・Git 管理外）
├── .env.example                  ANTHROPIC_API_KEY=your_api_key_here のみ
├── .claude/
│   ├── launch.json              uvicorn 起動用の launch 設定
│   └── settings.local.json      Claude Code の権限設定（アプリとは無関係）
├── CLAUDE.md / README.md / SQlite3_command.md / デプロイ.md / 進捗.md /
│   設計の背景.md / 中間発表_要旨構成メモ.md / 0901事前テストを終えて計画・修正.md   ← ドキュメント類
```

バックエンド合計 1,179 行（.py のみ）、フロントエンド合計 1,873 行。

### 1.2 起動方法

- `CLAUDE.md` 記載:
  ```
  cd backend && uvicorn main:app --reload --port 8000
  ```
- `.claude/launch.json` の設定:
  ```json
  {
    "name": "backend (FastAPI)",
    "runtimeExecutable": "backend/venv/bin/uvicorn",
    "runtimeArgs": ["main:app", "--reload", "--port", "8000", "--app-dir", "backend"],
    "port": 8000
  }
  ```
- `@app.on_event("startup")` で `database.init_db()` を呼び、DB とテーブルを自動作成・自動マイグレーションする。
- フロントは FastAPI が配信する。`/` → `frontend/index.html`、`/admin` → `frontend/admin.html`、`/static` に `frontend/` をマウント。
- 本番は Render（`デプロイ.md` による）。環境変数 `DATABASE_PATH=/data/sakumon.db`、`ANTHROPIC_API_KEY`、`$PORT` を Render 側で設定。公開URLは `https://sakumon.onrender.com/`（README・デプロイ.md 記載。稼働実績は未確認）。

### 1.3 依存関係

`backend/requirements.txt`:
```
fastapi
uvicorn[standard]
anthropic
python-dotenv
```
バージョン固定なし。`venv` は Python 3.14（`backend/venv/lib/python3.14/`、`__pycache__/*.cpython-314.pyc`）。フロントエンドは外部ライブラリ・ビルドツールなし（素の HTML/CSS/JS、SVG はJSで文字列生成）。

### 1.4 API エンドポイント一覧

すべて `backend/main.py` に定義。

| メソッド | パス | 用途 | 実装関数 |
|---|---|---|---|
| POST | `/api/login` | 学籍番号（2桁）検証 → その児童の過去セッション一覧を返す | `login()` |
| POST | `/api/session/new` | 新規セッション作成、`session_id` と式を返す | `new_session()` |
| POST | `/api/session/resume` | 既存セッションの履歴・作問・会話を返す（続きから/リロード復元） | `resume_session()` |
| POST | `/api/judge` | 児童の入力を分類→作問なら構造同定→声かけ生成。**主処理** | `judge()` |
| GET | `/` | `frontend/index.html` を返す | `index()` |
| GET | `/admin` | `frontend/admin.html` を返す | `admin_page()` |
| GET | `/admin/api/students` | 全児童の一覧（学籍番号・最終ログイン・達成構造数） | `admin_students()` |
| GET | `/admin/api/students/{user_id}` | 指定児童のセッション一覧 | `admin_student_detail()` |
| GET | `/admin/api/sessions/{session_id}` | 指定セッションの全チャットログ | `admin_session_logs()` |
| DELETE | `/admin/api/sessions/{session_id}` | セッションとそのログを物理削除 | `admin_delete_session()` |
| DELETE | `/admin/api/logs/{log_id}` | ログ1件を物理削除 | `admin_delete_log()` |
| GET | `/admin/api/export/csv` | 全ログを CSV でダウンロード | `admin_export_csv()` |
| （mount） | `/static/*` | `frontend/` を静的配信 | `StaticFiles` |

- CORS: `allow_origins=["*"], allow_methods=["*"], allow_headers=["*"]`（全開放）。
- 認証・APIキー・トークンの類は**一切なし**（管理者エンドポイント含む）。詳細は §8。
- 固定値: `EXPRESSION = "18 ÷ 3"`、`STRUCTURES = {"tobun", "hougan", "bai"}`、`USER_ID_PATTERN = ^[0-9a-z]{2}$`。

---

## 2. ai_judge.py

ファイル全体で 105 行。責務は「作問文の構造同定」に限定（docstring にも明記）。児童向けメッセージ・ヒント・表示種別・信号機状態は**生成しない**。

### 2.1 入力

`judge(message: str, expression: str) -> dict` の1関数のみ。

- `message`: 児童の入力文（`main.py` の `_handle_sakumon` から `req.message` がそのまま渡る）
- `expression`: 常に `"18 ÷ 3"`（`main.py` の `EXPRESSION` 定数）

会話履歴・信号機状態・直近ターンは**渡していない**（`ai_judge` は単発の文だけを見る）。

### 2.2 出力

戻り値は必ず次のいずれかの形の `dict`:

```python
# 成立
{"valid": True,  "structure": "tobun" | "hougan" | "bai", "issue": None}
# 不成立
{"valid": False, "structure": "invalid", "issue": "not_problem" | "wrong_number" | "reversed"}
# パース失敗（1回リトライ後も失敗）
{"valid": False, "structure": "invalid", "issue": "error", "error": "<例外文字列>"}
```

- `valid`: `bool`
- `structure`: `"tobun"` / `"hougan"` / `"bai"` / `"invalid"` のいずれか。`valid=False` のときは必ず `"invalid"`。
- `issue`: `valid=True` のとき `None`。`valid=False` のとき `"not_problem"` / `"wrong_number"` / `"reversed"` / `"error"`。
- LLM が返した JSON に `valid=true` でも `structure` が3構造以外なら、コード側で `{"valid": False, "structure": "invalid", "issue": result.get("issue") or "not_problem"}` に強制する（`ai_judge.py:98-99`）。

**「求める量」（1つ分・いくつ分・何倍）は返していない。** `ai_judge` の出力キーは `valid` / `structure` / `issue` / （失敗時 `error`）のみ。求める量に相当する情報は `ai_judge` のどこにも存在しない。
（参考: 求める量に近い概念は `ai_dialogue.py` の hint3 テープ図 `_tape_diagram_payload()` が返す `unknown` キー＝ `"1あたり量"` / `"いくつ分"` / `"倍"` のみ。§3.5 参照。）

### 2.3 3層判定（成立性 / 構造同定 / 方向確認）の実装状況

**3つの層は「別々の関数・別々のAPIコール」としては分離されていない。** 1回の `messages.create` 呼び出しで、1つのシステムプロンプト内に3観点を書き分け、1つの JSON `{valid, structure, issue}` を返させている。対応関係は次のとおり:

| 観点 | プロンプト上の記述 | 出力への反映 |
|---|---|---|
| 成立性 | 「## valid=false（成立していない）にすべき条件」節（誤字/脱字/文字化け・要素欠け・式が 18÷3 にならない） | `valid` = true/false。不成立時 `issue="not_problem"` または `"wrong_number"` |
| 構造同定 | 「## 3つの構造の定義」節（tobun/hougan/bai の定義と例） | `structure` = tobun/hougan/bai/invalid |
| 方向確認 | valid=false 条件の4項目め「bai（倍）で（大きい数）÷（小さい数）の向きが逆」 | 逆向きは `valid=false` + `issue="reversed"`（＝方向が逆なら**不成立扱い**。構造は返さない） |

補足: `README.md` は「ai_judge 第2層」「第1層」という表現を使うが、コード上に層構造の実体はない。方向が逆のとき `structure="bai"` は返らず `"invalid"` になる点に注意。

### 2.4 Anthropic API に渡しているプロンプト全文

**システムプロンプト**（`SYSTEM_PROMPT`。呼び出し時に `.replace("{expression}", expression)` で `{expression}` を `"18 ÷ 3"` に置換。`.format()` ではなく単純置換なので JSON 例の `{ }` はそのまま残る）:

```text
あなたは小学4年生の算数文章題の「構造」を同定するAIです。
児童が入力した文章題を読み、それが式 {expression} で解ける文章題として成立しているか、
成立しているならどの構造かだけを判定します。
児童向けのメッセージやヒントは書きません。判定結果のJSONだけを返してください。
JSON以外の文字は一切出力しないでください。

## 評価する式
{expression}

## 3つの構造の定義
- tobun（等分除）: 全体を等しく分けたとき「1人あたり・1つあたりいくつ」を求める問題。例:「18このあめを3人でわけると1人なんこ？」
- hougan（包含除）: 全体から一定数ずつ分けると「なん人分・なん組」になるかを求める問題。例:「18このあめを3こずつくばるとなん人にくばれる？」
- bai（倍）: ある数が別の数の何倍かを求める問題。例:「18mは3mのなんばい？」

## valid=false（成立していない）にすべき条件
- 誤字・脱字・文字化けなどで文章の意味が理解できない、または単語の羅列で文章になっていない
- 算数の文章題として必要な要素（誰が／何を／何こ／どうする／何を求めるか）が欠けている
- 文章を式にしたとき {expression} にならない（数値が違う・演算子が違う）
- bai（倍）で（大きい数）÷（小さい数）の向きが逆（例:「3こは18この何倍？」→ 3÷18 になる）

## issue（valid=false のときの理由コード。valid=true のときは null）
- "not_problem": 文章題として成立していない・要素が欠けている・意味が読めない
- "wrong_number": 式が {expression} にならない（数値・演算子がちがう）
- "reversed": 倍で大小の向きが逆になっている

## 返すJSONの形式（必ずこの形式のみ）
{
  "valid": true or false,
  "structure": "tobun" or "hougan" or "bai" or "invalid",
  "issue": null or "not_problem" or "wrong_number" or "reversed"
}
```

**ユーザーメッセージ**（`ai_judge.py:83`）:

```text
式: 18 ÷ 3
児童の入力: <児童の入力文そのまま>
```

### 2.5 API 呼び出しパラメータ・使用モデル

`ai_judge.py:19` / `:87-94`:

```python
MODEL = "claude-sonnet-5"

response = _client.messages.create(
    model=MODEL,                       # "claude-sonnet-5"
    max_tokens=256,
    thinking={"type": "disabled"},     # sonnet-5 は既定 on のため明示 off
    system=system,
    messages=[{"role": "user", "content": user_content}],
)
```

- リトライ: `for attempt in range(2)`（最大2回＝1リトライ）。
- レスポンスは `content` から最初の `type=="text"` ブロックを取り出す（`_text_from`）。先頭が ```` ``` ```` なら fence を剥がして `json.loads`（`_parse`）。
- APIキー: `anthropic.Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])`。`.env` は `load_dotenv(dotenv_path=<repo>/.env)` で読み込み。

**未確認**: `claude-sonnet-5` が API 側で実在・応答するか（本レポートでは API を叩いていない）。

---

## 3. ai_dialogue.py（＋前段の ai_classify.py）

### 3.1 発火条件（いつ呼ばれるか）

呼び出し経路は `main.py` の `judge()` → `ai_classify.classify()` で分岐:

```
POST /api/judge
  └─ ai_classify.classify(message, recent)  ← まず必ず1回。作問/対話を分類
       ├─ "sakumon" → _handle_sakumon()
       │       ├─ ai_judge.judge()                     ← 構造同定
       │       ├─ （issue=="error" なら ai_dialogue を呼ばず固定文言）
       │       └─ ai_dialogue.dialogue(kind="sakumon") ← 声かけ生成
       └─ "taiwa"   → _handle_taiwa()
               └─ ai_dialogue.dialogue(kind="taiwa")   ← 声かけ生成（ai_judge は通さない）
```

- `ai_dialogue.dialogue()` は **作問経路でも対話経路でも呼ばれる**（judge のエラー時のみ例外的に呼ばれない）。
- ただし `support_level == "hint3"` かつ未到達構造がある場合は、`_build_tape_diagram()` がコードだけで応答を組み立て、**LLM を一切呼ばない**（`ai_dialogue.py:264-269`。安全境界＝物語文を生成させないため）。

`ai_classify.classify()` の発火:
- `judge()` の入口で毎回1回。`recent`（直近6ターン、`get_recent_turns`）のうち末尾4件を文脈として渡す。
- 失敗時は安全側 `"taiwa"` に倒す（対話文が構造判定に流れ込むのを防ぐ）。
- モデル `claude-sonnet-5`、`max_tokens=64`、`thinking disabled`、最大1リトライ。

### 3.2 段階・レベルの概念（実装されているか / 何段階 / 遷移）

**実装されている。** 「支援の段階」= `support_level`（別名 `stumble` としてログにも記録）。値は次の7種類（`ai_dialogue.SUPPORT_LEVEL_JP` のキー）:

```
form / discover / hint1 / hint2 / hint3 / goal / talk
```

`support_level` を決めるのは `main.py`（決定論。AIには決めさせない）:

**作問経路** — `_sakumon_signals(valid, is_new, completes_all, issue, stall_count)`（`main.py:51-66`）:

| 条件 | support_level | display_type | stumble(ログ) |
|---|---|---|---|
| `valid=False` かつ issue="reversed" | `form` | `normal` | `reversed` |
| `valid=False` かつ issue="wrong_number" | `form` | `normal` | `wrong_expression` |
| `valid=False` かつ それ以外 | `form` | `normal` | `incomplete` |
| `valid=True` かつ 3構造そろった | `goal` | `clear` | `None` |
| `valid=True` かつ 新構造 | `discover` | `new_structure` | `None` |
| `valid=True` かつ 既出構造の反復 | `hint{min(3, stall_count)}` | 同左 | `hint1`/`hint2`/`hint3` |

- `stall_count = database.get_stall_count(session_id) + 1`。直近の新構造到達（`is_new=1`）より後、成立作問（`structure IS NOT NULL`）で同じ構造をくり返した回数（今回分を含む）。1→hint1、2→hint2、3以上→hint3。
- 遷移は「同じ構造を繰り返すたびに 1 段階上がり、hint3 で頭打ち。新しい構造を作った時点で discover に戻り、カウンタもリセット」。

**対話経路** — `_handle_taiwa()`（`main.py:161-189`）:

- `_taiwa_stumble(message)` がキーワードマッチで `"material_confusion"` / `"help_request"` / `None` を返す（`main.py:22-34`。決定論・研究再現性のための単純キーワード）。
  - `_MATERIAL_CONFUSION_KW`: 「同じ話」「おなじ話」「おなじはなし」「同じお話」「同じじゃ」「おなじじゃ」「変わらない」「かわらない」「いっしょ」「一緒」
  - `_HELP_KW`: 「わからない」「わかんない」「わからん」「こまった」「困った」「むずかしい」「難しい」「図」「おしえて」「教えて」「できない」「ヒント」「たすけ」「助け」
- stumble あり → `support_level = f"hint{_stuck_level(session_id, floor=2)}"`（＝ **hint2 か hint3 のみ**。児童が自分から混乱・ヘルプを表明した場合は hint1 を飛ばす）。
- stumble なし → `support_level = "talk"`。
- `_stuck_level(session_id, floor)` = `min(3, max(floor, get_stuck_streak(session_id) + 1))`。
  - `get_stuck_streak` = 直近の新構造発見（`is_new=1`）以降に積み上がった「足踏みシグナル」数（`stumble ∈ {hint1,hint2,hint3,material_confusion,help_request}`。間に雑談が挟まっても連続でなくてよい）。
- `display_type` は「stumble ありなら support_level そのもの（hint2/hint3）／なしなら `normal`」。

### 3.3 会話履歴の保持方法

- 永続化先は `chat_logs` テーブルのみ（会話専用テーブルはない）。1ターン＝児童入力1件 + AI応答（`response_json` 内）を1行に保存（`database.save_log`）。
- `ai_dialogue.dialogue()` に渡る履歴は2種類（いずれも `main.py` が DB から取得して引数で渡す。`ai_dialogue` 自身は DB を触らない）:
  - `history: list[str]` = `database.get_history(session_id)`。`is_new=1 AND structure IS NOT NULL` の DISTINCT structure。**到達済み構造（信号機の点灯状態）**。
  - `recent_turns: list[dict]` = `database.get_recent_turns(session_id, limit=6)`。直近6行を古い順に `[{"child": message, "ai": response_json.message, "input_type": ...}, ...]`。
- プロンプト内では `recent_turns` を `_build_history()` が
  ```
  子ども: <message>
  先生: <ai message>
  ```
  の平文に整形して埋め込む。**Anthropic の multi-turn `messages` 配列は使っていない。** 毎回 `messages=[{"role":"user","content": <その回の1本の文字列>}]` の単発呼び出しで、履歴はその文字列の中にテキストとして差し込む。
- フロント側は `sessionStorage` に `userId` / `sessionId` のみ保持。会話本文はリロード時に `/api/session/resume` → `database.get_conversation()`（全ターンを時系列で返す）で再取得して再描画。

### 3.4 Anthropic API に渡しているプロンプト全文（ai_dialogue）

**システムプロンプト** = `_RAW_PROMPT` の `{KANJI_RULE}` を `kanji_rule.KANJI_RULE` で単純置換したもの。

まず `_RAW_PROMPT`（`ai_dialogue.py:65-146`）:

```text
あなたは小学4年生が「わり算のお話づくり（作問）」をするのを助ける先生です。
子どもが同じ式（例：18÷3）から、いろいろな種類のお話を作れるように導きます。

# あなたのゴール
子どもに「数量関係の構造的理解」を深めさせること。
具体的には、同じ式でも【分ける話（等分除）】【分ける話（包含除）】
【くらべる話（倍）】という3つの違う構造があることに気づかせ、
まだ作っていない構造のお話を、子ども自身の力で作れるように促すこと。

# 3つの構造
- 等分除：全体を何人かに同じ数ずつ分けて「1人分」をさがす（□×3=18の□）
- 包含除：全体を何こずつかに分けて「何人分」をさがす（3×□=18の□）
- 倍　　：2つの大きさをくらべて「何倍」かをさがす（くらべる話）

# あなたが受け取る情報
- 子どもの発話
- この発話が「作問」か「対話」かの区別
- 直近の作問の判定結果（成立しているか／どの構造か／向きが正しいか）
- これまでに作れた構造のリスト（信号機の点灯状態）
- 直近のやりとりの履歴
- いまの「支援の段階」（下記のどれか）

# 支援の段階（最重要・これに従って声かけを変える）
- discover（新しい構造ができた）：まず称賛し、「何をさがしたか」を短い言葉で意味づける。
  ★ここでは、次に作る構造を絶対に名指ししない。答えの方向を渡さない。
  かわりに「ほかにどんなさがし方があるかな？」のように問いかけ、子ども自身に気づかせる。
- hint1（同じ構造を1回くり返した）：図はまだ出さない。
  「前のお話と、さがしているものは同じかな？」のように、くり返しに気づかせる
  問いかけだけにとどめる。次の構造の名前や求め方には触れない。
- hint2（くり返しが続く／「同じ話じゃないの？」「わからない」等）：図はまだ出さない。
  かわりに“比べる軸”そのものを転換させる。子どもは題材（りんご→えんぴつ等、話の
  中身）で「違うお話のつもり」になっていることが多い。題材ではなく「何をさがして
  いるか（数量関係）」で比べればさっきと同じだと気づけるよう、比較の軸を
  はっきり言葉で示す（例：「題材は変わったけど、さがしているものは同じかな？
  ちがうかな？」のように、比べる対象を“話の中身”から“さがしているもの”へ
  はっきり切りかえて問いかける）。次に何をさがせばよいかはまだ名指ししない。
- hint3（hint2でも変化がない／混乱がくり返される）：テープ図を提示する段階。
  この段階のメッセージと図はコード側が決定論的に生成するため、あなたが
  呼ばれることは通常ない。万一この段階であなたが呼ばれた場合も、図は出さず、
  hint2と同様に短く問いかけるだけにとどめる。
- form（お話が成立していない）：足りない要素を1つだけ問いかける。
- goal（3つの構造ができた）：3つとも答えは同じでも「さがすもの」が違うことを確認する。
- talk（その他の質問・つぶやき）：やさしく短く受け止め、作問にもどれるよう軽くうながす。
  ★ここで構造の名前や「何をさがすか」を教えてはいけない（それはhint1〜3の役目）。
  「どんな探し方がある？」と聞かれても、具体的には答えず、自分で考えるよう返す。

# 「同じ話じゃないの？」と言われたとき
題材で区別しようとして、構造で区別できていないサイン。support_levelは
通常hint2以上になっている。図は使わず、「何をさがしているか（さがし方）」で見れば
同じだと気づかせる問いかけで、比較の軸を題材から数量関係へ切りかえる。

# 向きが逆（倍の逆立式など）のとき
構造は合うが基準量と比較量を取りちがえている。どちらをもとにするかに気づかせる。

# 図を出す判断（figureフィールド）
支援は【言葉が主役・図は補助】。基本は短い言葉で導く。次のときだけ figure に構造名を入れる。
- discover：今できた構造（"tobun"/"hougan"/"bai"）を入れる。
- hint1：null（図はまだ出さない）。
- hint2：null（図はまだ出さない。この段階は図ではなく“比較の軸”を言葉で転換させる段階）。
- hint3：null（この段階のテープ図はコード側が決定論的に生成する。あなたは出さない）。
- goal："all"。
- form / talk：常に null。図を出すかどうかはmain.pyが決めるhint1〜3にまかせ、
  ここでは絶対に出さない。

# 文字づかいのルール
{KANJI_RULE}

# その他のルール（厳守）
- 1〜2文で短く。長い説明はしない。
- 答え（数や、完成した問題文そのもの）は絶対に教えない。
- 「だれが・なにを・なんこ・どうする」のような穴うめの型を与えない。
- hint3でも、そのまま書き写せば問題文になってしまうような完成した一文は渡さない。
  「何をさがすか」という視点のちがいだけを短く示し、文づくりは子どもに残す。
- やさしく、はげます口調。

# 出力（JSONのみ）
{
  "message": "子どもへの声かけ（1〜2文）",
  "figure": "tobun" | "hougan" | "bai" | "all" | null,
  "target_structure": "次に作ってほしい構造（tobun/hougan/bai）または null",
  "state": "読み取った子どもの状態（ログ用）"
}
```

`{KANJI_RULE}` に差し込まれる文字列（`kanji_rule.py:7-9`）:

```text
小学4年生までに習う漢字は必ず漢字で書く（倍・分・数・全部・同じ・時・回・人・何・話・作る・分ける・求める・考える など）。
習っていない漢字はひらがなで書く（こ〈個〉・まい〈枚〉 など）。
ひらがなを使いすぎない。
```

**ユーザーメッセージ** — `dialogue()` 内の f-string（`ai_dialogue.py:271-281`）:

```text
子どもの発話: <child_message>
この発話の区別: <"作問" または "対話">
いまの支援の段階: <SUPPORT_LEVEL_JP[support_level] の説明文（下記）>

直近の作問の判定結果:
<_build_situation() の出力（下記）>

これまでに作れた構造（信号機の点灯状態）: <_history_labels() の出力>

直近のやりとりの履歴:
<_build_history() の出力（「子ども: …」「先生: …」の平文）>
```

`SUPPORT_LEVEL_JP`（`ai_dialogue.py:51-63`。「いまの支援の段階:」に丸ごと差し込まれる）:

```text
form  : form（お話が成立していない：足りない要素を1つだけ問いかける）
discover: discover（新しい構造ができた：称賛＋意味づけ。★次の構造は名指しせず、問いかけで気づかせる）
hint1 : hint1（同じ構造を1回くり返した：図はまだ出さない。前と同じかどうかに気づかせる問いかけのみ）
hint2 : hint2（くり返しが続く、または『同じ話じゃないの？』『わからない』と言われた：図はまだ出さない。かわりに“比べる軸”を転換させる。題材（りんご／えんぴつ等）で比べるのをやめて、「何をさがしているか（数量関係）」で比べるよう、比較の軸を言葉ではっきり示す）
hint3 : hint3（hint2でも変化がない／混乱がくり返される：テープ図を提示する段階。このメッセージ・図はコード側が決定論的に生成するため、通常ここにはこない）
goal  : goal（3つの構造ができた：さがすものの違いを確認する）
talk  : talk（その他の質問・つぶやき：やさしく短く受け止め、作問にもどれるようにうながす）
```

`_build_situation(input_kind, jr)`（`ai_dialogue.py:157-173`。「直近の作問の判定結果:」に差し込まれる。取りうる文字列）:

```text
（対話 or jr なし）    : これは対話（質問・つぶやき・こまった等）です。新しい作問ではありません。
（valid=false, reversed）: 作問したが、倍の向きが逆（基準量と比較量を取りちがえている）。式が18÷3にならない。
（valid=false, wrong_number）: 作問したが、式が18÷3にならない（数値や演算がちがう）。
（valid=false, その他）  : 作問したが、文章題として成立していない（要素が欠けている・意味が読めない）。
（valid=true, completes_all）: 作問成立。構造は<ラベル>。これで3つの構造がすべてそろった（ゴール到達）。
（valid=true, is_new）  : 作問成立。構造は<ラベル>。これは新しく作れた構造。
（valid=true, 既出）    : 作問成立。構造は<ラベル>。ただしこの構造はすでに作ったことがある（同じ構造のくり返し・停滞）。
```
`<ラベル>` は `STRUCTURE_JP` = `{"tobun":"等分除（分ける話・1人分をさがす）", "hougan":"包含除（分ける話・何人分をさがす）", "bai":"倍（くらべる話）"}`。

### 3.5 ai_dialogue の出力形式

LLM 経由の戻り値（`ai_dialogue.py:305-311`）:

```python
{
    "message": str,                 # 児童向け声かけ（必須。無ければリトライ→フォールバック）
    "figure": "tobun"|"hougan"|"bai"|"all"|None,   # form/talk/hint1/hint2/hint3 ではコード側で強制 None
    "target_structure": "tobun"|"hougan"|"bai"|None,
    "state": str|None,              # AIが読み取った児童の状態（ログ用の自由記述）
    "tape_diagram": None,           # LLM経路では常に None
}
```

hint3（コード生成、LLM を呼ばない。`_build_tape_diagram` → `_tape_diagram_payload`。`ai_dialogue.py:216-249`）:

```python
{
    "message": "このテープ図に合うお話を考えてみよう。",   # TAPE_DIAGRAM_MESSAGE 固定
    "figure": None,
    "target_structure": "<未到達構造。tobun→hougan→bai の固定順で最初の未到達>",
    "state": "hint3_tape_diagram",
    "tape_diagram": {
        "type": "tape_diagram",
        "structure": "等分除" | "包含除" | "倍",          # STRUCTURE_LABEL_JA
        "known":   {"全体量":18,"いくつ分":3} |            # tobun
                   {"全体量":18,"1あたり量":3} |           # hougan
                   {"比較量":18,"基準量":3},               # bai
        "unknown": "1あたり量" | "いくつ分" | "倍",         # ← 「求める量」に相当する唯一のキー
    },
}
```
数値は `DIVIDEND=18` / `DIVISOR=3` 固定。3構造すべて到達済みで hint3 が来た場合は `_build_tape_diagram` が `None` を返し、通常の LLM 対話にフォールバックする。

失敗時フォールバック（`ai_dialogue.py:315-318`）:
```python
{"message": "もう一度 おくってみてね", "figure": None, "target_structure": None,
 "state": "fallback", "tape_diagram": None}
```
（`FALLBACK_MESSAGE = "もう一度 おくってみてね"`。判定エラー時に `main.py` からも使われる。）

### 3.6 使用モデル・パラメータ（ai_dialogue / ai_classify）

| モジュール | MODEL | max_tokens | thinking | リトライ |
|---|---|---|---|---|
| ai_dialogue.py | `claude-sonnet-5` | 512 | `{"type":"disabled"}` | 最大1回 |
| ai_classify.py | `claude-sonnet-5` | 64 | `{"type":"disabled"}` | 最大1回 |
| ai_judge.py | `claude-sonnet-5` | 256 | `{"type":"disabled"}` | 最大1回 |

3モジュールとも `messages=[{"role":"user","content": <単一文字列>}]` の単発呼び出し。モデル名はソース直書き（環境変数化されていない）。

**ai_classify のシステムプロンプト全文**（`ai_classify.py:19-35`）:

```text
あなたは、小学4年生が「わり算のお話づくり（作問）」をするアプリの入力仕分け係です。
児童が今おくった入力を、「作問」か「対話」かに分類することだけが仕事です。
構造の同定や正誤判定はしません。判定結果のJSONだけを返してください。
JSON以外の文字は一切出力しないでください。

## 分類の定義
- "sakumon"（作問）: 新しい文章題（お話）を作ろうとしている入力。
  例:「18このあめを3人でわけると1人なんこ？」「りんごが18こある。3こずつくばると何人にくばれる？」
- "taiwa"（対話）: 質問・つぶやき・こまった・あいさつなど、お話づくりそのものではない入力。
  例:「これでいいの？」「同じ話じゃないの？」「わからない」「図で見たい」「むずかしい」「つぎどうするの？」

## 迷ったときの目安
- 数量と問い（〜は何こ？ など）がそろった文章題の形なら "sakumon"。
- 短い質問・感想・困りごと・あいさつは "taiwa"。

## 返すJSON（この形式のみ）
{ "type": "sakumon" or "taiwa" }
```

ai_classify のユーザーメッセージ（`ai_classify.py:57-67`）:
```text
これまでのやりとり:
子ども: <直近ターンの child>
先生: <直近ターンの ai>
（…末尾4件まで。無ければこの見出しごと省略）

今の入力: <児童の入力文>
```

---

## 4. データベース（data/sakumon.db）

- 実体は SQLite3。パスは `os.environ["DATABASE_PATH"]` があればそれ、無ければ `<repo>/data/sakumon.db`（`database.py:7-8`）。
- `PRAGMA journal_mode=WAL` / `PRAGMA busy_timeout=30000` / `sqlite3.connect(..., timeout=30)`。
- 起動時 `init_db()` が `CREATE TABLE IF NOT EXISTS` 2本 + `_migrate()` を実行。
- アプリ定義テーブルは **`sessions` と `chat_logs` の2つのみ**（ほかに SQLite 内部の `sqlite_sequence`）。

### 4.1 CREATE TABLE 文（実DBの `.schema` 出力そのまま）

```sql
CREATE TABLE sessions (
                session_id INTEGER PRIMARY KEY AUTOINCREMENT,
                user_id TEXT NOT NULL,
                expression TEXT NOT NULL,
                created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
            );

CREATE TABLE sqlite_sequence(name,seq);

CREATE TABLE chat_logs (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                session_id INTEGER NOT NULL,
                user_id TEXT NOT NULL,
                message TEXT NOT NULL,
                response_json TEXT NOT NULL,
                structure TEXT,
                is_new INTEGER NOT NULL DEFAULT 0,
                created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
            , input_type TEXT, stumble TEXT);
```

補足:
- `chat_logs` の `input_type` / `stumble` は `database.py` の `CREATE TABLE` 本文には無く、`_migrate()`（`database.py:47-55`）が `ALTER TABLE ... ADD COLUMN` で後付けする。だから `.schema` では末尾にぶら下がる。
- インデックス定義・外部キー制約・`ON DELETE` は**一切なし**。`session_id` の整合はアプリ任せ（削除は `admin_delete_session` が両テーブルを手動 DELETE）。
- `created_at` は列定義に `DEFAULT CURRENT_TIMESTAMP` があるが、実際の INSERT では `datetime.utcnow()` を明示的に渡している（`create_session` / `save_log`）。つまり値は **UTC のナイーブ文字列**（例 `2026-08-04 22:32:20.475913`）。フロント/管理画面は末尾に `Z` を付けて UTC としてパースし、ローカル時刻表示する。

### 4.2 各テーブル・各カラムの保持内容

**sessions** — 1行 = 1回の「学習セッション」（＝児童が「新しく始める」を押した単位）

| カラム | 型 | 内容 |
|---|---|---|
| `session_id` | INTEGER PK AUTOINCREMENT | セッションの一意ID。フロントが以降の API 呼び出しで使う実質的な鍵 |
| `user_id` | TEXT NOT NULL | 児童の学籍番号（2桁 `[0-9a-z]`）。ログイン画面の入力値をクライアントで小文字化したもの |
| `expression` | TEXT NOT NULL | 対象の式。現状すべて `"18 ÷ 3"`（`create_session` に `EXPRESSION` を渡している） |
| `created_at` | TIMESTAMP | セッション作成時刻（UTC ナイーブ）。「最終ログイン」表示にも流用 |

**chat_logs** — 1行 = 児童の1入力とそれに対する応答1組（作問・対話の両方）

| カラム | 型 | 内容 |
|---|---|---|
| `id` | INTEGER PK AUTOINCREMENT | ログの一意ID。時系列ソートキー（`ORDER BY id`） |
| `session_id` | INTEGER NOT NULL | 属するセッション |
| `user_id` | TEXT NOT NULL | 児童の学籍番号（`sessions.user_id` の非正規化コピー） |
| `message` | TEXT NOT NULL | **児童が入力した生テキスト** |
| `response_json` | TEXT NOT NULL | `main.py` が返した応答 dict を `json.dumps(ensure_ascii=False)` した文字列。中身は `valid`/`structure`/`is_new`/`display_type`/`message`（＝**AIの発話**）/`figure`/`target_structure`/`state`/`tape_diagram`/`input_type` |
| `structure` | TEXT | 成立作問なら `tobun`/`hougan`/`bai`。不成立・対話は `NULL` |
| `is_new` | INTEGER NOT NULL DEFAULT 0 | そのセッションで初めて到達した構造なら `1`。信号機の点灯判定に使う |
| `input_type` | TEXT | `"sakumon"` / `"taiwa"`。旧データは `NULL`（マイグレーション前） |
| `stumble` | TEXT | つまづき種別（研究分析用）。§4.4 参照。旧データは `NULL` |
| `created_at` | TIMESTAMP | 応答保存時刻（UTC ナイーブ） |

`response_json` の実例（DB 実データより）:

```json
// id=7 新構造（作問）
{"valid": true, "structure": "tobun", "is_new": true, "display_type": "new_structure",
 "message": "いいね！18こを3人で同じ数ずつ分けて、1人分をさがすお話ができたね。次は「3こずつ」分けると「何人分」になるか、というお話にも挑戦してみよう！",
 "figure": "tobun", "target_structure": "hougan", "state": "新しい構造ができた（等分除）", "input_type": "sakumon"}

// id=53 hint3（テープ図・コード生成）
{"valid": true, "structure": "tobun", "is_new": false, "display_type": "hint3",
 "message": "このテープ図に合うお話を考えてみよう。", "figure": null, "target_structure": "hougan",
 "state": "hint3_tape_diagram",
 "tape_diagram": {"type": "tape_diagram", "structure": "包含除", "known": {"全体量": 18, "1あたり量": 3}, "unknown": "いくつ分"},
 "input_type": "sakumon"}

// id=20 対話（旧コードのため figure が入っている。現行コードでは taiwa の figure は必ず null）
{"valid": false, "structure": null, "is_new": false, "display_type": "hint2",
 "message": "題材はちがうけど、この図を見て。…",
 "figure": "tobun", "target_structure": null, "state": "hint2：…", "input_type": "taiwa"}
```

### 4.3 現在の各テーブルの行数

`data/sakumon.db` + `sakumon.db-wal` + `sakumon.db-shm` をスクラッチにコピーして計測（WAL 反映済みの現在値）:

| テーブル | 行数 |
|---|---|
| `sessions` | **25** |
| `chat_logs` | **72** |
| `sqlite_sequence` | 2 行（`sessions` の seq=27、`chat_logs` の seq=72） |

補足:
- `sessions` は seq=27 に対し 25 行 → 過去に 2 セッションが削除されている。
- `chat_logs` は seq=72＝72 行（欠番なし）。
- `sessions` の `user_id` は 11 種（`12, 13, 21, 32, 33, aa, t1, t2, t3, te, zz`）。うち `t1/t2/t3/zz` は明らかにテスト用ID。
- `chat_logs` があるのは 17 セッション・8 ユーザーのみ（残りは入力ゼロで終了）。
- 期間: `sessions.created_at` 2026-06-29 〜 2026-08-04。`chat_logs.created_at` 2026-06-29 〜 2026-08-04。**9月のデータは無い。**
- `chat_logs` 内訳: `input_type` = sakumon 36 / taiwa 30 / NULL 6。`structure` = tobun 31 / hougan 3 / bai 1 / NULL 37。`is_new` = 1 が 19 / 0 が 53。
- `stumble` 内訳: NULL 49 / `repeat_structure` 9 / `help_request` 5 / `hint1` 4 / `material_confusion` 2 / `hint2` 1 / `hint3` 1 / `incomplete` 1。→ **`repeat_structure` は現行コードが書かない値（旧コードの残骸）**。現行コードは既出構造反復に `hint1`/`hint2`/`hint3` を書く。

### 4.4 児童識別キーとセッションの結びつき

- **児童の識別キー = `user_id`（学籍番号・2桁 `[0-9a-z]`）だけ**。パスワード・トークン・サーバ側セッションIDなし。
- ログイン（`POST /api/login`）は正規表現チェックのみ。DB に照会するのは「その `user_id` の過去セッション一覧を返す」ため。未登録の `user_id` でも 200 が返り、空リストになる（＝任意の2桁を打てば誰にでもなれる）。
- `sessions.user_id` に一意制約なし。1児童が複数セッションを持てる（実データで最大7セッション）。
- `chat_logs` は `session_id` と `user_id` の**両方**を持つ（`user_id` は `sessions` からの非正規化コピー。`save_log` の引数で渡している）。
- セッションの所有チェックは**ない**。`/api/session/resume` と `/api/judge` は `session_id` の存在確認（`get_session_user` が `None` でないか）だけで、その `session_id` が呼び出し元の児童のものかは検証しない。整数を総当たりすれば他人のセッションを閲覧・追記できる。
- フロントは `sessionStorage` に `userId` / `sessionId` を保存。**ページのリロード時のみ**（`performance` の navigation type が `reload`）復元。それ以外（タブを開き直す等）は `clearSession()` される。

---

## 5. フロントエンド

### 5.1 画面数と役割

**児童向け `index.html` — 1ファイル内に5つの `<div class="screen">`。JS の `showScreen(id)` で表示を切り替える（SPA的）。**

| # | id | 役割 |
|---|---|---|
| 1 | `screen-login` | ログイン。出席番号（半角英数2桁）入力。`18 ÷ 3` を大きく表示 |
| 2 | `screen-choose` | 「新しく始める」／「前回の続きから」。過去セッションがあれば表示、なければ即新規作成 |
| 3 | `screen-sessions` | 過去セッション一覧。日付・問題数・到達構造（`等分除・包含除` 等の日本語ラベル）を出し、選んで再開 |
| 4 | `screen-game` | 作問画面。左＝チャット、右＝メーターパネル（後述）。主画面 |
| 5 | `screen-clear` | クリア画面。3構造達成時。作った問題を構造ラベル付きで一覧、「もう一度やる」 |

**管理者向け `admin.html` — 1ファイル内に2ビュー。`showView(id)` で切替。**

| ビュー | 役割 |
|---|---|
| `view-students` | 児童一覧（学籍番号・最終ログイン・達成構造数 `n/3` バッジ）。「CSVダウンロード」ボタン |
| `view-student-detail` | 選んだ児童のセッション一覧。各セッションを展開するとチャットログ表（日時・種別・入力/AI返答・構造・新規・つまづき・削除）を遅延ロード |

### 5.2 信号機UIの実装箇所とラベル

**実装箇所**: `frontend/index.html:75-84`（`screen-game` 右パネル内の `.panel-card`）と、点灯ロジック `frontend/index.js` の `updateMeter()`（`:418-435`）、CSS は `frontend/index.css` の `.lights` / `.light` / `.light.on` / `.lights-label`（`:332-362`）。

**HTML 構造**:
```html
<div class="panel-card">
  <h3>違うお話</h3>
  <div class="lights">
    <div class="light" id="light-0">⭐</div>
    <div class="light" id="light-1">⭐</div>
    <div class="light" id="light-2">⭐</div>
  </div>
  <div class="lights-label" id="lights-label">あと 3 つ</div>
</div>
```

**ラベルとして表示しているもの**:
- パネル見出し = 固定文字列 **「違うお話」**。
- 3つのランプ = いずれも絵文字 **⭐** のみ。中に「等分除」等の構造名は**入っていない**。個々のランプに構造名・色・番号などのラベルは付かない。
- ランプ下のテキスト `#lights-label` = **「あと N つ」**（N = `3 - history.length`）、全部点いたら **「全部できた！」**。
- 点灯条件: `updateMeter()` が `state.history`（＝到達済み構造名の配列）の**個数**だけを見て、先頭から順に `i < count` のランプに `.on`（緑＋グロー）を付ける。**どのランプがどの構造か、という対応は持たない**（3個目の構造が「倍」でも、点くのは3番目のランプというだけ）。

構造名（等分除／包含除／倍）が児童に見える箇所は信号機ではなく、次の3つに限られる:
- セッション一覧画面（`renderSessionList`：`STRUCTURE_LABEL[x]`）
- クリア画面（`showClear`：問題ごとに `<span class="tag">等分除</span>` 等）
- テープ図カードのタイトル（`FIGURE_TITLE`：例「分ける話：1つ分をさがす（等分除）」「くらべる話：何ばい？（倍）」）— figure/tape_diagram が返ったときのみ表示

作問中の「作った問題」リスト（`addProblem`）は問題文テキストのみで、構造名は出さない。

### 5.3 判定結果を児童にどう表示しているか

`index.js` の `sendMessage()`（`:451-494`）が `/api/judge` の応答を受けて描画する。

- 児童の入力は即 `addUserBubble()`（右寄せ吹き出し）。送信中は `addLoadingBubble()`（3点ドット）。
- AIの声かけ `data.message` を `addAiBubble(text, data.display_type)` で左寄せ吹き出し表示。`display_type` → CSS クラス対応（`:204-217`）:
  | display_type | class | 見た目（`index.css:246-268`） |
  |---|---|---|
  | `new_structure` | `new-structure` | 緑背景・太緑枠・太字 |
  | `hint1` / `hint2` / `hint3` | `hint` | 黄背景・オレンジ枠 |
  | `clear` | `clear` | 緑背景・太枠・大きめ太字 |
  | それ以外（`normal` 等） | `normal` | グレー背景 |
- `data.valid` が真なら `addProblem(text, data.structure)` で右パネル「作った問題」に追加、`updateMeter()` で信号機・「作った数」を更新。
- `data.figure`（`tobun`/`hougan`/`bai`/`all`）があれば `addFigureCard()` が `figureSvg()` の決定論的 SVG テープ図をチャットに差し込む。`all` は3構造ミニ図を並べる。
- `data.tape_diagram`（hint3 の JSON）があれば `addTapeDiagramCard()` が `tapeDiagramSvg()` で SVG 描画（未知量が「長さ型」か「個数型」かで `？` の描き方を変える作り込みあり。`:309-404`）。
- `display_type === "clear"` のときは吹き出し＋（あれば figure）を出したあと `setTimeout` で `showClear()` に遷移（figure ありで 3200ms、無しで 1800ms）。
- **判定の生の内訳（`valid` の理由、`issue`、`structure` 名そのもの、`is_new`、`support_level`）を明示表示する UI は無い。** 児童が受け取るのは「吹き出しの色」＋「信号機の点灯数」＋「figure/テープ図が出るか」だけ。`structure` 名の文字列は作問中は画面に出ない（§5.2）。

管理画面側（`admin.js` `buildLogRow`）は、種別バッジ（作問/対話）・構造バッジ（等分除等）・新規バッジ・つまづきバッジ（`STUMBLE_LABEL`）・「📊 図: …」タグを表形式で見せる。`STUMBLE_LABEL` は `{incomplete:"要素不足", wrong_expression:"式ちがい", reversed:"向き逆", hint1:"停滞1(気づきの問い)", hint2:"停滞2(比較の軸)", hint3:"停滞3(テープ図)", material_confusion:"題材混同", help_request:"助け求め"}`。

### 5.4 表示のオン/オフを切り替える仕組みがあるか

**無い。** 調べた範囲（`index.html` / `index.js` / `index.css` / `main.py` / `admin.*`）に、判定結果・信号機・声かけ・figure の表示を切り替えるフラグ、クエリパラメータ、設定項目、環境変数、UI トグルは存在しない。信号機パネルは `screen-game` に常時描画される。`display_type` による見た目の出し分けはあるが、これは「どのスタイルで出すか」であって「出す/出さない」ではない。

---

## 6. フェーズ・モードの概念

- **「フェーズ」「モード」に相当する全体状態は無い。** セッション・ユーザー・システムのいずれにも「今は第1段階／第2段階」のような列・変数・設定は存在しない。
- 入力ごとに分岐する概念は2つだけ:
  1. `kind`（`ai_classify` が返す `"sakumon"` / `"taiwa"`）— そのターン限りの経路分岐。
  2. `support_level` / `display_type`（`form/discover/hint1/hint2/hint3/goal/talk` と `normal/new_structure/hint1..3/clear`）— そのターン限りの支援段階。`main.py` が DB のカウント（`get_stall_count` / `get_stuck_streak`）から毎回計算する。永続的な「モード」ではない。
- **全ユーザに一斉に何かを切り替える仕組みは無い。** グローバル設定テーブル、フィーチャーフラグ、`/admin` からの挙動変更エンドポイントのいずれも存在しない。`EXPRESSION = "18 ÷ 3"` を変えるにはソース修正＋再起動が必要（しかも `index.html` 3箇所・`index.js` の SVG・`ai_dialogue.py` の `DIVIDEND/DIVISOR` にもハードコードが散在）。
- **管理者用の画面・エンドポイントはある**（`/admin`, `/admin/api/students`, `/admin/api/students/{user_id}`, `/admin/api/sessions/{session_id}` GET/DELETE, `/admin/api/logs/{log_id}` DELETE, `/admin/api/export/csv`）。ただし機能は**閲覧・CSV出力・削除のみ**。教師が授業中に「次の段階へ」と切り替えるような制御機能は無い。認証も無い（§8）。

---

## 7. ログ

### 7.1 何がどこに記録されるか

すべて `chat_logs` テーブル1本（`database.save_log`）。1入力＝1行。

| 情報 | 記録先 |
|---|---|
| 児童の入力（生テキスト） | `chat_logs.message` |
| 入力の種別（作問/対話） | `chat_logs.input_type`（`"sakumon"`/`"taiwa"`） |
| 構造同定結果 | `chat_logs.structure`（`tobun`/`hougan`/`bai`/NULL）、詳細は `response_json.valid` / `response_json.structure` |
| 新規到達フラグ | `chat_logs.is_new`（0/1） |
| つまづき種別 | `chat_logs.stumble`（`incomplete`/`wrong_expression`/`reversed`/`hint1`/`hint2`/`hint3`/`material_confusion`/`help_request`/NULL） |
| AIの発話 | `chat_logs.response_json` 内の `message` キー（**専用カラムは無い**） |
| 表示種別 | `response_json.display_type` |
| 出した図 | `response_json.figure` / `response_json.tape_diagram` |
| AIが読み取った児童状態（自由記述） | `response_json.state` |
| 次に促す構造 | `response_json.target_structure` |
| セッション情報 | `sessions`（`user_id`, `expression`, `created_at`） |

- 保存タイミング: `_handle_sakumon` / `_handle_taiwa` / judge エラー時の3経路すべてで最後に `save_log` を呼ぶ。**判定・声かけが返れば必ず1行残る**（入力ゼロや通信失敗前の未送信は残らない）。
- CSV エクスポート（`admin_get_all_logs_csv`）が展開するフィールド: `user_id, session_id, session_start, created_at, message, ai_message, structure, is_new, input_type, stumble, figure, state, session_new_count`。

### 7.2 タイムスタンプ

- **記録されている。** `chat_logs.created_at` と `sessions.created_at`。いずれも `datetime.utcnow()`（UTC・ナイーブ・マイクロ秒付き文字列、例 `2026-08-04 22:32:20.475913`）。
- 粒度は「1入力ごと」。開始〜送信の思考時間、1文字ごとの入力過程などは残らない。
- タイムゾーン情報（`+09:00` や `Z`）は文字列に含まれない。表示側で `Z` を付けて UTC 解釈している。

### 7.3 記録されていない重要情報（改修時の検討候補として指摘）

- **`ai_classify` の分類結果そのもの・信頼度**: `input_type` に間接的に残るだけ。分類ミス（対話を作問と誤判定 等）の事後分析ができない。
- **`ai_judge` の生 `issue`**: `not_problem` / `wrong_number` は `stumble` に `incomplete` / `wrong_expression` と改名されて残るが、`response_json` に `issue` キーは含まれない。judge の生 JSON は保存されない。
- **`support_level`（form/discover/goal/talk）**: `stumble` は反復（hint1-3）とキーワード検知（material_confusion/help_request）のときしか入らない。discover・goal・form・talk のターンは `stumble=NULL` で、`response_json.display_type` から逆算するしかない。
- **`stall_count` / `stuck_streak` の値**: 毎回再計算で、その時点の数値は残らない。
- **LLM のメタ情報**: モデルバージョン・入力/出力トークン数・レイテンシ・停止理由・リトライ有無・生レスポンス текст は一切残らない。
- **セッションの結末**: `showClear()` は完全にクライアント側処理で、DB に「クリアした」フラグは無い。3構造到達は `is_new` 3種から推定するしかなく、「クリア画面まで到達したか」「途中離脱か」は判別不可。`sessions` に終了時刻も無い。
- **ログイン／再開イベント**: `POST /api/login` `POST /api/session/resume` はログを残さない。`sessions.created_at` の流用で「最終ログイン」と呼んでいるだけ（実際は最終セッション作成時刻）。
- **意図的作り分けの直接指標**: `0901事前テストを終えて計画・修正.md` が主要指標に挙げる「別の型で作ろうと促された直後に別構造を出せたか」は、現状ログからは「hint後の次の成立作問の `structure` を追う」形で**間接的にしか**復元できない。促し（target_structure 提示）と次入力の対応を明示的に記録する仕組みは無い。
- **削除の記録**: `admin_delete_session` / `admin_delete_log` は物理削除。監査ログ・論理削除フラグ無し。
- **クライアント環境**: IP・UA・端末・画面種別など無し（研究用途では通常不要だが念のため）。

---

## 8. その他

### 8.1 未使用コード・実験的な放置箇所

- **`chat_logs.stumble = "repeat_structure"`（実データ9行）**: 現行コードは既出構造の反復に `hint1`/`hint2`/`hint3` を書く（`main.py:_sakumon_signals`）。`repeat_structure` を書くコードは現存しない＝旧実装の遺物。管理画面の `STUMBLE_LABEL` にもこのキーは無く、生文字列でバッジ表示される。
- **`input_type = NULL`（実データ6行）**: マイグレーション前のデータ。管理画面・CSV 集計は `input_type IS NULL` を「作問扱い」にフォールバックしている（`database.py:251`）。
- **`_stuck_level(session_id, floor=1)` の `floor=1` 経路**: 実際の呼び出しは `_handle_taiwa` からの `floor=2` のみ（`main.py:167`）。デフォルト `floor=1` と `max(1, streak)` の分岐は事実上デッド。
- **対話経路の figure**: `_handle_taiwa` は `dlg.get("figure")` を結果に載せるが、`ai_dialogue.dialogue` は `support_level ∈ {form,talk,hint1,hint2,hint3}` で figure を強制 `None` にする。対話経路の `support_level` は必ずこのいずれかなので、**現行コードでは taiwa の figure は常に None**。実データ id=20 の `figure:"tobun"` は旧コード時代のもの。
- **`ai_dialogue._RAW_PROMPT` の figure 節と hint3 の記述**: 「hint3 で万一呼ばれたら…」の分岐はフォールバック保険で、通常経路では `_build_tape_diagram` が先に処理して LLM を呼ばない。
- **`data/sakumon.db-wal` が約515KBで未チェックポイント**: 72行のわりに肥大。運用上の実害は薄いが、バックアップを `.db` だけコピーすると WAL 分を取りこぼす（実際 `data/` に `.db.backup_*` が2つ、`.db-wal` 抜きで置かれている）。
- **`data/sakumon.db.backup_20260721_160059` / `..._20260722_230542`**: Git 未追跡の手動バックアップ。自動バックアップの仕組みはコードに無い（`デプロイ.md` は「管理画面の CSV を手元へ」と案内）。
- **ルート直下のドキュメント8本**: `設計の背景.md`・`中間発表_要旨構成メモ.md`・`0901事前テストを終えて計画・修正.md` は Git 未追跡。`0901…` は 9/18 改修の設計方針（S0〜S3 の学習者モデル、段階化した課題提示、主要指標の差し替え）を述べたもので、**現行コードには未反映**。

### 8.2 認証・児童識別の仕組み

- **児童**: 出席番号2桁（`^[0-9a-z]{2}$`）のみ。パスワード・ワンタイム・サーバセッション・Cookie いずれも無し。`/api/login` は形式チェックのみで、任意の2桁で誰にでもなりすませる。セッション所有権チェック無し（`session_id` を知る／総当たりで他人のセッションを閲覧・追記可能）。
- **管理画面**: `/admin` および `/admin/api/*` は**完全に無認証**。URL を知っていれば誰でも全児童のログ閲覧・CSV ダウンロード・セッション/ログの物理削除が可能。
- **CORS**: `allow_origins=["*"]`（全オリジン許可）。
- クライアントの状態保持は `sessionStorage`（`userId` / `sessionId`）のみ。リロード時だけ復元。

### 8.3 環境変数・設定ファイル

| 変数 / ファイル | 用途 | 既定 / 備考 |
|---|---|---|
| `ANTHROPIC_API_KEY` | Anthropic クライアント初期化（3モジュール共通）。`os.environ["ANTHROPIC_API_KEY"]`（未設定なら起動時 KeyError） | `.env`（1行のみ・Git 管理外）／本番は Render の Environment |
| `DATABASE_PATH` | DB ファイルパス上書き（`database.py:7`） | 未設定なら `<repo>/data/sakumon.db`。`デプロイ.md` では Render で `/data/sakumon.db` を設定 |
| `$PORT` | uvicorn 待受ポート | コードには無し。Render が渡す（`デプロイ.md`）。ローカルは `--port 8000` 手動指定 |
| `.env.example` | サンプル | `ANTHROPIC_API_KEY=your_api_key_here` の1行のみ（`DATABASE_PATH` は記載なし） |
| `.claude/launch.json` | uvicorn 起動設定（エディタの実行構成） | アプリ本体の設定ではない |
| `.claude/settings.local.json` | Claude Code の許可コマンド一覧 | アプリと無関係 |
| モデル名 | `MODEL = "claude-sonnet-5"` を `ai_judge.py` / `ai_dialogue.py` / `ai_classify.py` にハードコード | 環境変数化されていない |
| 式・数値 | `EXPRESSION="18 ÷ 3"`（main.py）、`DIVIDEND=18`/`DIVISOR=3`（ai_dialogue.py）、`"18 ÷ 3"` 文字列（index.html×3, index.js の SVG） | すべて直書き。変更は多ファイル修正が必要 |

`.gitignore`: `.env` / `__pycache__/` / `*.pyc` / `*.db` / `*.db-shm` / `*.db-wal` / `venv/` / `.venv/` / `.claude/`。→ DB とバックアップはリポジトリに入らない（本番 Render の永続ディスク上のみが本番データ）。

---

## 9. 未確認・調査範囲外の項目

- `claude-sonnet-5` を含む Anthropic API の実挙動（本調査では API を1回も呼んでいない。プロンプト・パラメータはソース記載どおり）。
- 本番 `https://sakumon.onrender.com/` の稼働状況・そこの DB 内容（ローカル `data/sakumon.db` のみ調査）。
- `frontend/index.css` / `admin.css` の全内容（信号機・吹き出し関連の該当箇所のみ精読。レイアウト全般は未精読）。
- `backend/venv/` 内の実インストール済みパッケージのバージョン（`requirements.txt` は未固定。`venv` 実体は未列挙）。
- ドキュメント `SQlite3_command.md` / `進捗.md` / `中間発表_要旨構成メモ.md` / `Gitignoreについて.md` の内容（改修の前提として §1・§8 に必要な範囲のみ参照）。
- Git 履歴の全容（各バックエンドファイルの直近1コミットのみ確認。`ai_judge.py`・`ai_classify.py` は 2026-07-23 の `52cf2c5` 以降変更なし、`main.py`・`ai_dialogue.py`・`database.py` は 2026-09-05 の `f94c1e1` が最新）。
