# 9月18日 授業実践：運用手順（教師・システム担当用）

## 0. 前日までに（Render／ローカル共通）

1. **環境変数 `ADMIN_PASSWORD` を必ず設定する。** 未設定だとサーバは起動しない。
   - Render：Dashboard → Environment に `ADMIN_PASSWORD=（任意の文字列）` を追加してから Deploy。
   - ローカル：`.env` に `ADMIN_PASSWORD=...`（`.env.example` 参照）。
2. デプロイ後、`https://sakumon.onrender.com/admin` を開き、ブラウザの認証ダイアログに
   **ユーザー名は何でもよい／パスワードに ADMIN_PASSWORD** を入れて入れることを確認。
3. 管理画面「フェーズ管理」で **式A（フェーズ1・2）と式B（フェーズ3）** を入力して「式を保存」。
   （初期値 A=24 ÷ 4、B=18 ÷ 3。「整数 ÷ 整数」でわり切れる式のみ受け付ける）
4. リハーサルをした場合は、当日の朝に **「新しい回を始める」** を押す。
   run 番号が進み、フェーズ1に戻る。児童は次のログインから新しいセッションになる（リハーサルのデータは消えない）。
5. Render 無料枠はしばらくアクセスが無いとスリープするので、授業10分前に一度アクセスして起こしておく。

## 1. 授業の流れ（2コマ・85分）

| 時間 | 操作（教師） | 児童の画面 |
|---|---|---|
| 開始 | フェーズが **1** になっていることを確認 | 出席番号を入れて「次へ」。式Aが表示され、入力欄と送信ボタンだけ。送ると「おくったよ」 |
| 15分後 | 「フェーズ2」ボタン → 確認ダイアログ OK | 全端末に「ここまでで いったん おしまい」が約3秒出て、同じ画面のまま支援が始まる（入力中の文は消える） |
| 50分後 | 「フェーズ3」ボタン → OK | 「いったん おしまい」→ 式Bの新しい画面（履歴・信号機はリセット）。送ると「おくったよ」 |
| 終了 | 「児童一覧・ログ」タブ → 「CSVダウンロード」 | ― |

- 切替は児童側の操作なしで、**最大5秒**で全端末に反映される（5秒ポーリング）。
- 誤って別のフェーズを押した場合は、正しいフェーズを押し直せばよい（1・2は同じセッション、3は別セッション。データは消えない）。
- 児童が誤ってログアウト／リロード／タブを閉じても、出席番号を入れ直せば **同じセッションに戻る**。

## 2. 授業中に教師が見るもの（フェーズ管理タブの「児童の状態」）

- 5秒ごとに自動更新。
- **赤い行 ＝ 今のフェーズで提出0問**。手が止まっている児童なので机間指導で拾う（システムは無入力を検出しない）。
- 「接続」：直近15秒以内に通信があれば接続中。
- 「到達」：●●● は左から 1つ分（等分除）／いくつ分（包含除）／何倍（倍）。
- 「支援水準」：フェーズ2のみ。0=成立性、1=一覧、2=同じ？、3=求める量、4=場面。
- 「学習者状態」：S0 産出破綻／S1 単一構造／S2 複数構造／S3 意図的作り分け（S2・S3は暫定）。

## 3. 児童の画面で起こること（フェーズ2）

- 不成立の作問 → 成立性の声かけ（水準0）。
- 既出の構造をくり返すたびに水準が1段階上がる：
  1. 今までのお話の一覧　2. 一覧＋「同じかな？ちがうかな？」＋2択ボタン　3. 「1つ分／いくつ分／何倍」の3つの聞き方＋信号機　4. 場面（素材）の足場かけ
- 新しい構造が出たら称賛のみ（次の構造は言わない）。水準は1に戻る。
- 「作った お話」リストは水準1以降、信号機は水準3以降に右側に出る。
- 3つそろうと「3つとも作れたね」。構造名（等分除など）は児童には一切見せない。図は出ない。

## 4. 分析用データ

- CSV の主な列：`phase`（1/2/3）、`support_level`（none / form / level1〜4 / discover / goal / talk / error）、
  `learner_state`、`structure`、`unknown`（one_unit / num_units / ratio / base / rate）、`issue`、
  `is_new`、`button_pressed`（押したボタン。複数なら「同じ,ちがう」）と `message`（実際の送信文）、`stall_count`、`expression`。
- 「水準Nの声かけ直後の提出で新構造が出たか」＝ 同じセッション内で直前行の `support_level` と当該行の `is_new`・`structure` を見る。
- フェーズ切替の時刻は DB の `phase_changes` テーブルに残る。

## 5. トラブル時

- 児童の画面に「もう一度 ログインしてね」→ 出席番号を入れ直す。
- 判定が返らない（「もう一度 おくってみてね」）→ API側の一時的な失敗。もう一度送ればよい。ログには `error` として残る。
- 管理画面に入れない → `ADMIN_PASSWORD` を確認。ブラウザを閉じて開き直すと認証ダイアログが再表示される。
